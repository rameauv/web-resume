{
  "name": "web-resume",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently --kill-others \"cd ./client && npm run build && serve -s build\" \"cd backend && npm start\""
  },
  "dependencies": {
    "concurrently": "^5.0.0",
    "enzyme": "^3.11.0",
    "jest": "^24.9.0"
  }
}
